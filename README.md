# To run backend:
    First, cd into backend folder.
### Powershell: 
    .\mvnw.cmd spring-boot:run

### Command Prompt:
    mvnw.cmd spring-boot:run    

# Dependencies:
### Backend:
java version 25 or older
### Frontend:
node.js